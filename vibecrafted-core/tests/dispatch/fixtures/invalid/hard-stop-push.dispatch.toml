schema = "vibecrafted.dispatch.v1"

[meta]
name = "hard-stop-push"
repo = "/tmp/vibecrafted-dispatch-fixture"

[[cuts]]
id = "c1"
agent = "codex"
workflow = "implement"
prompt = "Do it."

  [[cuts.verify]]
  run = "git push --force origin HEAD"
  expect = { exit_code = 0 }
